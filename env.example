# WireBot Configuration
# Copy this file to .env and fill in your values

# Telegram Bot Token (get from @BotFather)
BOT_TOKEN=your_bot_token_here

# Owner Telegram User ID (your user ID)
OWNER_ID=your_user_id_here

# Optional: Additional authorized users (comma-separated user IDs)
# AUTHORIZED_USERS=123456789,987654321

# Optional: WireGuard script path (default: ./wireguard.sh)
# WIREGUARD_SCRIPT_PATH=/path/to/wireguard.sh

# Optional: WireGuard config path (default: /etc/wireguard/wg0.conf)
# WIREGUARD_CONFIG_PATH=/etc/wireguard/wg0.conf

# Optional: Maximum clients per user (default: 100)
# MAX_CLIENTS_PER_USER=100

# Optional: Rate limit per user per minute (default: 10)
# RATE_LIMIT_PER_USER=10
